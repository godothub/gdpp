## 2.0.1

- 支持更高版本的编辑器回退低版本API。
- 修复扩展反复初始化失败和错误的导出缺库警告。

## 2.0.0

- 采用全新 AOT 引擎，提升 Godot 4.4～4.7 项目的 GDScript 兼容性、运行性能与稳定性。
- 优化导出体验，支持并行编译、链接时优化、实时进度、取消构建与更清晰的错误诊断。

## 1.9.0

- 内置 Ninja 1.13.2 构建后端，无需用户额外配置即可进行受 CPU 和内存约束的并行编译、顺序链接与增量复用。
- 每次导出重新生成完整的当前 C++ 工程状态，同时保留未变化文件，由 Ninja 统一管理依赖和零工作量重建。
- 在隔离工作线程运行预编译和原生构建，并向保持响应的编辑器界面持续上报单调的逐文件编译与链接进度。
- 完善 Windows 导出中的 MSVC 环境、隐藏进程输出、UTF-8 响应文件、Unicode、长路径和 Emscripten 命令处理。
- 加强中断恢复、iOS XCFramework 事务、内置 Ninja 校验和无源码发行边界。

## 1.8.4

- 新增适用于 Godot 4.6～4.7 的 `gdpp-lite.zip`，包含 macOS、Windows 编辑器以及桌面、Android、Web 导出支持，不包含 Linux 和 iOS 载荷。
- 隔离编辑器、导出和着色器缓存变化，并原子复制必要元数据，提升重复导出的稳定性。

## 1.8.3

- 新增面向 Godot 4.6～4.7 的 `gdpp.zip` 和面向 Godot 4.4～4.7 的 `gdpp-all.zip`，覆盖全部受支持编辑器与导出平台。
- 统一跨脚本类、继承、内部类、枚举、常量、静态调用、方法覆盖和名称冲突的项目级身份。
- 在生成跨脚本 ABI 前完成字段、常量、参数、默认参数依赖和协程契约推断。
- 在路径、UID、线程加载、`load`/`preload`、属性、信号、构造、附着、继承、回滚和关闭过程中保持编译后 Script 身份。
- 完善 Callable、强类型 Array 与 Dictionary、枚举、PackedArray、嵌套写入、动态 `len`、运算和转换语义。
- 保持异步初始化、`await super`、协程生命周期以及脚本替换或关闭时的安全取消。
- 在纯二进制导出中保留外部脚本、场景、嵌套资源、序列化引用、导出排除项、预设、诊断和客户源码。
- 提升生成静态状态、Script 缓存、附着实例和不同编译器原生分支的跨平台启动与关闭安全性。

## 1.8.2

- 支持上下文关键字迭代变量和左花括号位于下一行的枚举声明。
- 统一全局脚本类、内部类和嵌套枚举在类型标注、构造、成员访问、容器和类型测试中的解析。
- 将命名脚本枚举保留为只读 Dictionary，并为导出 `Variant` 属性保留正确的运行时与 Inspector 元数据。
- 在加载、属性、信号、对象传递、类型测试和构造过程中保持编译后 Script 资源的规范状态身份。
- 修复 Godot 属性访问、初始化器求值顺序、扩展回滚和 MSVC、Clang、GCC 下的关闭行为。

## 1.8.1

- 修复所有分支均 `return`、`break` 或 `continue` 的嵌套 `if`/`else` 控制流误报 `GDS5118`。
- 在优化后保持控制流操作身份确定，并在不产生过量内存分配的前提下拒绝损坏的控制流图。

## 1.8.0

- 将生成项目库的入口统一为 `gdpp_library_init`，并在升级时清理废弃的项目描述符与 CMake 工程文件。
- 支持可挂起静态函数、强类型协程 lambda、并发恢复以及与原生 GDScript `await` 兼容的引用计数 FunctionState 结果。
- 匹配 GDScript 的 lambda 捕获、名称遮蔽、共享容器身份、默认参数、可变参数、返回 Callable 和异步循环行为。
- 将致命脚本操作限制在当前生成调用内，同时保持调用方执行、源码顺序、惰性求值和准确的 `.gd` 诊断。
- 匹配 Dictionary 缺失键、已存储 null、强类型键值、只读、点号访问、直接赋值和复合赋值语义。
- 对 Variant、Object、Ref、Array、Dictionary、PackedArray 和附着属性执行严格运行时存储转换，不再静默生成默认值。
- 优化精确整数、Variant、Dictionary、Callable 和转换路径，同时保持 AOT 性能回退不超过商业合同规定的 10%。
- 让静态、preload、字段、`_init` 和 `@onready` 初始化具备事务性，并加强空对象、已释放对象、`Object.free()` 和附着实例生命周期安全。
- 完整支持 `breakpoint`，并为方法、访问器、lambda、继承成员、附着类和挂起协程提供源码级调试帧。
- 改进解析恢复、大项目工作线程栈、确定性 MIR 校验和优化失败回滚。
- 保持运行时路径、UID、ResourceLoader 调用、缓存检查、`exists()`、`get_script()` 和 `.new()` 的动态编译 Script 身份。
- 完善 ENet 重连与清理、异步赋值和访问器、自定义 Godot API、双精度以及 SDK/运行时兼容性校验。

## 1.7.10

- 匹配附着脚本继承 `@onready` 初始化与重复 `request_ready()` 生命周期，且不遮蔽用户 `_ready` 回调。
- 为 Array 和全部 PackedArray 的读取、写入与复合赋值增加支持负索引的越界检查和源码定位诊断。
- 在强类型和动态对象调用、属性、键、迭代器、setter 与分量回写前拒绝空对象和已释放对象。
- 保持 PackedArray 元素类型、共享存储、Signal 与 Callable 参数、本地信号行为和生成方法直接分派。
- 跟踪真实生成头文件依赖，使实现修改只重建必要文件，公开脚本变化仍会重建全部依赖项。
- 在帧或信号挂起后保持协程循环变量状态，并在警告洁净的 C++ 中保留合法的未使用 GDScript 绑定。
- 对启动、帧运行和各类基准执行 AOT 相对 GDScript 最多回退 10% 的商业性能合同。

## 1.7.9

- 使用仓库自有 Node.js 24 Action 替换已弃用的 Node.js 20 MSVC 初始化依赖，并严格校验 Visual Studio x64 工具链。
- 通过 `vswhere` 查找 Visual Studio，支持 Preview 和非默认安装，保留显式编译器设置并调用正确的同目录链接器。
- 将桌面、Android、iOS 和 Web 的客户生成库文件名从 `gdpp_project` 缩短为 `gdpp`，并事务式清理旧产物。
- 恢复标准 `addons/gdpp/` 压缩包目录，兼容 Godot 4.7.1，并消除严格客户项目中的 Variant 推断警告。
- 导出期间保持编辑器与供应商描述符不变，避免无效热重载，并且只注册一次生成项目库。
- 通过统一注册流程保留正确的 Android ABI、Web 线程模式、桌面目标位置、iOS 入口和第三方供应商打包契约。
- 新增真正的 Universal 2 macOS compiler、fallback 和 Godot 4.4～4.7 桌面 SDK，支持 Apple Silicon 与 Intel 编辑器和导出。
- 改进中断事务恢复，并且只识别 Godot 4.6.2 已知的精确 iOS 模板警告，不隐藏其他诊断。

## 1.7.8

- 将脚本常量和资源预加载推迟到 Godot 实际请求时执行，避免导出游戏在引擎服务启动期间失败。
- 保持描述符注册仅处理元数据，并在不运行客户代码的情况下校验延迟常量、继承合并、重复名称和缺失解析器。
- 恢复根类和内部附着类构造，通过强类型描述符 getter/setter 分派字段并支持自定义访问器。
- 让静态函数 Callable 可作为信号目标，并在强类型 Array、Dictionary、默认值和跨脚本返回中保持准确的附着 Script 身份。
- 按实际 getter 与 setter 类型处理 Godot 属性，覆盖多态 Shader、材质、粒子、天空、雾、几何体、灯光、贴花和相机资源。
- 拆分属性 getter 与 setter 契约，使赋值和复合回写使用引擎真实的写入侧类型。
- 让桌面宿主组件与编译器、Godot、Android、Web 和 iOS 任务并行构建，全部成功后再进行最终组包。
- 共享 SDK 头文件、源码和运行时数据，仅保留平台、架构、配置和 Web 模式专用库，减少重复内容。

## 1.7.7

- 将源码扫描、解析、语义分析、原生生成、编译和链接移到后台工作线程，保持编辑器响应。
- 仅在导出开始时编译兼容 GDScript，且只构建选定的 Debug 或 Release 目标；常规编辑和编辑器内运行继续使用原始脚本。
- 使用一次直接导出构建和一个生成项目库替代客户 CMake 工程、编辑器预构建、第二描述符和热重载链。
- 通过附着脚本行为和仅限导出的 ScriptLanguage 元数据桥接保持 Godot 与第三方 GDExtension 对象为真实所有者，无需修改源码。
- 保持跨脚本字段、方法、Autoload、内部类、继承、`is`/`as`、`self`、供应商回调、属性、信号和序列化默认值。
- 修复 JSON 与 HTTP 响应及嵌套更新中的 Dictionary 点号访问，使异步登录和网络流程保留正确字段值。
- 在别名、参数、返回、默认值、Callable、lambda、Signal、下标、迭代和动态调用中保持全部 PackedArray 的共享存储。
- 按被调用方真实 ABI 实体化跨脚本与引擎调用参数，并为容器、供应商、工具函数和可变参数使用一致 Variant 适配。
- Debug 与 Release 导出共用一套优化客户绑定，并将发行内容整合为包含受支持移动端和 Web SDK 的桌面插件包。
- 改进 Windows 构建稳定性、逐文件进度、绘制刷新、可安装插件布局以及描述符与 Godot 扩展扫描的隔离。

## 1.7.6

- 在全部桌面主机上保持原生构建窗口位于当前 Godot 导出对话框之前。
- 使用一条连续且单调递增的 AOT 进度条替代分配置和列式进度显示。
- 将进度分配到扫描、解析、分析、脚本预编译、原生生成、编译和链接，并继续细分到单个文件。
- 使用简洁的 AOT、Debug、Release 文案和实时文件计数，不暴露后端转译术语。

## 1.7.5

- 修复 Windows MSVC 环境初始化，确保 `vcvars64.bat`、`cl.exe` 和带引号路径正确执行。
- 串行执行生成翻译单元的编译与链接，避免导出期进程突发和内存争用。
- 在每个原生文件完成后推进进度，同时保持 Godot 编辑器响应。
- 隐藏工具链窗口，并在全部桌面主机的导出诊断中显示有限的 stdout、stderr、失败文件、阶段和退出码。

## 1.7.4

- 在全部商业插件包中包含原生构建进度界面，并拒绝内容不完整的发行压缩包。
- Debug 与 Release 导出统一使用优化的 `template_release` 客户绑定，同时保留 Godot 所需的私有编辑器绑定。
- 移除全部客户 `template_debug` 归档，消除重复调试对象并显著减小插件包体积。
- 保留 Debug 导出的 `assert()` 语义，同时对生成项目库使用 Release 优化、死代码移除和符号隐藏。
- 新增包含 macOS、Linux、Windows 编辑器及桌面、Android、iOS、Web 导出 SDK 的完整分 Godot 版本插件包。
- 在编译或组包前拒绝过期、混合、缺失、生成、嵌套或使用 Debug 绑定的 SDK 内容。

## 1.7.3

- 消除 Windows 导出期间编译器、环境初始化和链接器子进程的可见控制台窗口。
- 使用直接原生构建 API 进行受控编译和顺序链接，移除无用的客户 CMake 导出开关。
- 在 Godot 打包前显示独立原生构建进度，覆盖扫描、解析、分析、原生生成、编译和链接。
- 在不同构建配置间保持阶段顺序和单调进度，并在进入 Godot 打包进度前移除原生进度界面。

## 1.7.2

- 完整反射第三方 GDExtension 的 Variant、强类型容器、Signal、对象和编码元数据契约，无需修改源码。
- 按生成原生 ABI 修复内部类 override、`super`、默认参数、可变参数、协程和动态分派。
- 在声明、参数、返回、容器和跨脚本引用中保持嵌套枚举身份。
- 完整比较附着脚本描述符，并加强 Callable 赋值的自别名安全性。
- 在构建前拒绝不支持的 Windows arm64、Linux arm64 和 Android x86_64 目标，并加强 Windows SDK 校验。

## 1.7.1

- 完善 Godot 4.4～4.7 的强类型可变参数函数、构造器、方法、lambda、反射、调用、缓存和附着分派。
- 新增覆盖根类、内部类、枚举、常量、静态成员、强类型资源、转换、类型测试和继承的跨脚本 preload 命名空间。
- 改进跨版本 RefCounted、Object、singleton、属性、null、动态调用和协程 ABI 处理。
- 解析 Autoload UID，保留运行时资源图，并在纯二进制导出中隔离编辑器专用脚本。
- 保护 Dictionary、容器、字符串、路径、PackedArray、Callable 和 Signal 赋值，避免自赋值损坏和原生资源滞留。
- 改进跨平台编译器与 SDK 传递，并在导出前拒绝不兼容的运行时、CRT、异常、平台、架构和配置组合。

## 1.7.0

- 新增无需重建或修改供应商插件即可继承独立第三方 GDExtension 类的零源码改动 AOT 支持。
- 将生成 ScriptInstance 行为附着到供应商持有的原生对象，覆盖字段、方法、属性、信号、通知、虚回调和 RPC 元数据。
- 保持第三方原生根类之上的脚本继承、初始化、访问器、信号和 RPC 覆盖。
- 通过反射得到的稳定 GDExtension ABI 调用外部 `super`，不使用不安全的跨动态库 C++ 继承。
- 在纯二进制导出中转换场景、资源、内嵌资源和 Autoload，同时保留供应商类型、状态、描述符和动态库。
- 改进供应商加载顺序、缓存失效、macOS Universal 校验、崩溃恢复和 SDK 完整性检查。

## 1.6.0

- 完善 Variant、可空性和真值语义，并贯通分析、强类型 IR、生成 C++17 和原生运行时。
- 为数值、字符串、内建值、PackedArray、Object、Ref、RID、Array 和 Dictionary 定义严格赋值、转换、构造和存储规则。
- 新增参数化 Array/Dictionary 转换、跨脚本枚举转换、受保护运行时转换和不可能转换的稳定诊断。
- 校验强类型容器元素、键、值、对象类和脚本元数据，不再依赖宽松的原生隐式转换。
- 在纯二进制导出中保留抽象原生脚本契约，并为全部平台 SDK 更新受保护转换与存储运行时。

## 1.5.0

- 新增类型测试、空值与真值检查、短路逻辑、分支、循环、条件表达式、守卫和结构化 `match` 的流敏感类型分析。
- 在分支间保留有效类型、存储类型和非空证明，并在重新赋值或 Callable 边界后使不安全推断失效。
- 为静态解析的方法和属性增加带源码位置的空对象与已释放对象保护。
- 完善省略参数、方法覆盖、参数数量检查和 Godot 虚函数的默认参数与 Callable 元数据。
- 实现 `assert`、`is_instance_of`、`type_exists`、`convert`、`str_to_var` 和 `var_to_str` 等 GDScript 工具函数。
- 执行静态上下文规则，对未解析符号失败关闭，并在生成表达式中保持从左到右的副作用顺序。

## 1.4.0

- 统一常量求值、优化、生成强类型代码和动态运行时的整数语义。
- 确定性处理溢出、移位、除余、复合赋值和原生范围，消除 C++ 未定义行为。

## 1.3.0

- 新增浮点及 Vector2/Vector2i/Vector3/Vector3i 范围的原生 `for` 循环，并匹配 Godot 的方向、步长和边界。
- 新增强类型自定义迭代协议，并严格校验数组、字典和循环变量的上下文类型。

## 1.2.0

- 完整支持强类型 Array 与 Dictionary，贯通分析、跨脚本接口、导出属性、嵌套容器和生成 C++。
- 校验容器存储并直接构造强类型字面量，减少分配、转换和包装拷贝。

## 1.1.0

- 扩展字符串、数字、Unicode 标识符、局部常量、尾随逗号和 lambda 等现代 GDScript 语法支持。
- 新增数组、字典、嵌套和剩余项 `match` 模式，并贯通分析、优化和原生生成。
- 完善 `await` 与协程返回值、立即完成、并发调用和继承动态分派。
- 改进多脚本依赖跟踪、增量构建、异常输入安全、诊断和跨平台导出设置。

## 1.0.0

- 首个版本，提供 GDScript AOT 编译器和 Godot 编辑器插件。
